<div class="ImageHero--Wrap">
    <div class="ImageHero {% if section.settings.align_l == 'right' %}ImageHero--ContentRight{% endif %}
                                   {% if section.settings.align == 'left' %}ImageHero--ContentLeft{% endif %}"
         {% if section.settings.image %}style="background: url({{ section.settings.image_l | img_url: '1x1' }})"{% endif %}>
        <div class="ImageHero__ImageWrapper">
            <div class="ImageHero__Image {% if section.settings.overlay_opacity > 0 %}ImageHero__ImageWrapper--hasOverlay{% endif %} Image--lazyLoad Image--zoomOut"
                 data-optimumx="1.4"
                 data-expand="-150"
                 style="background-image: url({{ section.settings.image_l | img_url: '700x' }})">
            </div>

            <noscript>
                <div class="ImageHero__Image"
                     style="background-image: url({{ section.settings.image_l | img_url: '700x' }})"></div>
            </noscript>
            {%- unless section.settings.image != blank -%}
                <div class="PlaceholderBackground PlaceholderBackground--Dark">
                    {{ 'lifestyle-1' | placeholder_svg_tag: 'PlaceholderBackground__Svg' }}
                </div>
            {%- endunless -%}
        </div>
        <div class="ImageHero__Content ImageHero__Content--{% if section.settings.image_l != blank %}Image{% endif %}">
            {%- if section.settings.l_subheading != blank or section.settings.l_title != blank or section.settings.l_content != blank -%}
                {%- if section.settings.l_subheading != blank or section.settings.l_title != blank -%}
                    <header class="SectionHeader SectionHeader--MiddleIndent {% if section.settings.align_l == 'right' %}TextRight BlockRight{% endif %}
                                   {% if section.settings.align_l == 'left' %}TextLeft BlockLeft{% endif %}">
                        {%- if section.settings.l_subheading != blank -%}
                            <h3 class="SectionHeader__SubHeading Heading u-h6">{{ section.settings.l_subheading | escape }}</h3>
                        {%- endif -%}

                        {%- if section.settings.l_title != blank -%}
                            <h2 class="SectionHeader__Heading Heading u-h1">{{ section.settings.l_title | escape }}</h2>
                        {%- endif -%}

                        {%- if section.settings.l_content != blank -%}
                            <div class="SectionHeader__Description">
                                {{ section.settings.l_content }}
                            </div>
                        {%- endif -%}
                        {%- if section.settings.l_link_title != blank -%}
                            <div class="SectionHeader__Link">
                                <a class="link Link--underline" href="{{ section.settings.l_link }}" >{{ section.settings.l_link_title }}</a>
                            </div>
                        {%- endif -%}

                    </header>
                {%- endif -%}
            {%- endif -%}
        </div>
    </div>
</div>
<style>
    .ImageHero--Wrap {
        width: 100%;
    }
    .ImageHero__Content {
        position: relative;
        flex-basis: auto;
        padding: 20px 40px 10px 40px;
    }
    .SectionHeader {
        margin-bottom: 25px;
    }
    .ImageHero--ContentRight{
        text-align: right;
        height: 100%;
        align-items: center;
    }
    .ImageHero--ContentLeft{
        text-align: left;
        height: 100%;
        align-items: center;
    }
    .SectionHeader.SectionHeader--MiddleIndent {
        margin-bottom: 30px;
    }
    .ImageHero__Content .ImageHero__Content--Image {
        padding: 0;
    }
    @media (min-width: 981px) {
        .ImageHero__Content {
            padding: 40px 80px 20px 80px;
        }
    }
    .SectionHeader__Link {
        padding: 40px 0;
    }
  .ImageHero__Content--Image {
       width: 100%;
  }
</style>